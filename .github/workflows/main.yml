name: Hello Program

on:
  push:
    branches: [ dev ]

jobs:
  build:
    runs-on: [self-hosted, Linux, X64]

    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v2
      with:
        node-version: '10'
    - run: npm install
    - name: Run npm start
      run: npm start
